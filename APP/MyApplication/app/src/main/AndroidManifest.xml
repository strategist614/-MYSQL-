<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Register"></activity>
        <activity android:name=".Commodity_type" />
        <activity android:name=".Add_product" />
        <activity android:name=".layout_my_commodity" />
        <activity android:name=".ModifyInfo" />
        <activity android:name=".MyinfoActivity" />
        <activity android:name=".MyCommodityActivity" />
        <activity android:name=".AboutApp" />
        <activity android:name=".CommonType" />
        <activity android:name=".Personalcenter" />
        <activity android:name=".MainActivity" />
        <activity android:name=".loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>